# Camera Ball Game

The interactive build now lives in TouchDesigner:

- `touchdesigner/CameraBallGame.toe`
- `touchdesigner/run_camera_ball_game.bat`

The localhost Vite app is only a clean launcher/status page. The actual camera game runs in TouchDesigner using `Video Device In TOP -> Script TOP(OpenCV) -> Out TOP`.
